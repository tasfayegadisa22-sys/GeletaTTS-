name: Build GeletaTTS APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle 8.4
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.4

      - name: Fix Folder Structure and Organize Files
        run: |
          # 1. ፋይሎቹ GeletaTTS- በሚል የተደራረበ ፎልደር ውስጥ ከሆኑ አውጥቶ ዋናው ገጽ ላይ ያደርጋቸዋል
          if [ -d "GeletaTTS-" ]; then
            echo "የተደራረበ ፎልደር ተገኝቷል። ፋይሎቹን ወደ ውጭ በማውጣት ላይ..."
            mv GeletaTTS-/* . 2>/dev/null || true
            mv GeletaTTS-/.* . 2>/dev/null || true
          fi

          # 2. ዚፕ ፋይሉን ፈልጎ መፍታት እና ማደራጀት
          ZIP_PATH=$(find . -iname "lang.zip" | head -n 1)
          if [ -n "$ZIP_PATH" ]; then
            unzip -o "$ZIP_PATH" -d temp_lang/
            mkdir -p app/src/main/assets/geleta-tts-data/
            find temp_lang/ -type f -exec cp -v {} app/src/main/assets/geleta-tts-data/ \;
            rm -rf temp_lang/
          fi

      - name: Build APK
        run: |
          # gradlew ፋይል ያለበትን ፈልጎ ወደ ዋናው ገጽ ማምጣት
          GRADLEW_PATH=$(find . -name "gradlew" | head -n 1)
          if [ -n "$GRADLEW_PATH" ]; then
            cp "$GRADLEW_PATH" ./gradlew
          else
            gradle wrapper --gradle-version 8.4
          fi
          
          chmod +x ./gradlew
          ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: GeletaTTS-App
          path: "**/build/outputs/apk/debug/*.apk"
