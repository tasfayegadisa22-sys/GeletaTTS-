name: Build GeletaTTS APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Extract Voice Data and Organize
        run: |
          if [ -f "lang.zip" ]; then
            unzip -o lang.zip -d temp_lang/
            mkdir -p app/src/main/assets/geleta-tts-data/
            find temp_lang/ -type f -exec cp {} app/src/main/assets/geleta-tts-data/ \;
            rm lang.zip
            rm -rf temp_lang/
          else
            echo "lang.zip አልተገኘም፣ ወደ ቀጣዩ ስራ ያልፋል..."
          fi

      - name: Commit Organized Files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add app/src/main/assets/geleta-tts-data/
          git diff --quiet && git diff --staged --quiet || (git commit -m "Organized voice files from zip" && git push) || true

      - name: Build with Gradle
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: GeletaTTS-App
          path: app/build/outputs/apk/debug/app-debug.apk
